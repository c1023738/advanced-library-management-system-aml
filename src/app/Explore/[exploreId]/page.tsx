import client from "@/lib/wix";

export default async function ItemPage(){
//     params,
//   }: {
//     params: { exploreId: string };
//   }) {
//     const { data: Inventory } = await client.items.getDataItem(params.exploreId, {
//         dataCollectionId: "Inventory",

//     });

    return{
        
    }
  }